[tool.poetry]
name = "frontend"
version = "0.1.0"
description = ""
authors = ["you"]

[tool.poetry.dependencies]
python = "^3.11"
streamlit = "^1.45.1"
pillow = "^10.3.0"
requests = "^2.32.3"
pandas = "^2.2.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"